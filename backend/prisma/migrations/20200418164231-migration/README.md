# Migration `20200418164231-migration`

This migration has been generated by marcel at 4/18/2020, 4:42:31 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200418163541-migration..20200418164231-migration
--- datamodel.dml
+++ datamodel.dml
@@ -8,9 +8,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 enum Discipline {
     POWERLIFTING
@@ -44,10 +44,10 @@
     createdAt               DateTime    @default(now())
     updatedAt               DateTime    @updatedAt
     name                    String
-    discipline              Discipline  @default(POWERLIFTING)
-    contestType             ContestType @default(SINGLE)
+    discipline              Discipline
+    contestType             ContestType
 }
 model Athlete {
     id                      String      @id @default(uuid())
@@ -56,9 +56,9 @@
     raw                     Boolean
     athleteNumber           Int
     firstName               String
     lastName                String
-    gender                  Gender      @default(MALE)
+    gender                  Gender
     club                    String
     birthday                DateTime
     total                   Float
     norm                    Boolean
@@ -118,9 +118,9 @@
 model Attempt {
     id                      String      @id  @default(uuid())
     createdAt               DateTime    @default(now())
     updatedAt               DateTime    @updatedAt
-    discipline              Discipline  @default(SQUAT)
+    discipline              Discipline
     date                    DateTime
     index                   Int
     weight                  Int
     raw                     Boolean
@@ -136,9 +136,9 @@
     id                      String      @id  @default(uuid())
     createdAt               DateTime    @default(now())
     updatedAt               DateTime    @updatedAt
     name                    String
-    gender                  Gender      @default(MALE)
+    gender                  Gender
     min                     Float
     max                     Int
 }
@@ -170,9 +170,9 @@
 model OfficialSlot {
     id                      String      @id  @default(uuid())
     createdAt               DateTime    @default(now())
     updatedAt               DateTime    @updatedAt
-    position                Position    @default(SEITENKAMPFRICHTER)
+    position                Position
     official                Official    @relation(fields: [officialId], references: [id])
     officialId              String
     slot                    Slot        @relation(fields: [slotId], references: [id])
```

# Migration `20200418163541-migration`

This migration has been generated by marcel at 4/18/2020, 4:35:41 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200418111310-migration..20200418163541-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,12 +1,16 @@
 generator ts {
   provider = "prisma-client-js"
-  output   = "./src/client"
+  output   = "../api/client"
 }
+generator prisma_client {
+  provider = "prisma-client-js"
+}
+
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 enum Discipline {
     POWERLIFTING
@@ -40,10 +44,10 @@
     createdAt               DateTime    @default(now())
     updatedAt               DateTime    @updatedAt
     name                    String
-    discipline              Discipline
-    contestType             ContestType
+    discipline              Discipline  @default(POWERLIFTING)
+    contestType             ContestType @default(SINGLE)
 }
 model Athlete {
     id                      String      @id @default(uuid())
@@ -52,9 +56,9 @@
     raw                     Boolean
     athleteNumber           Int
     firstName               String
     lastName                String
-    gender                  Gender
+    gender                  Gender      @default(MALE)
     club                    String
     birthday                DateTime
     total                   Float
     norm                    Boolean
@@ -114,9 +118,9 @@
 model Attempt {
     id                      String      @id  @default(uuid())
     createdAt               DateTime    @default(now())
     updatedAt               DateTime    @updatedAt
-    discipline              Discipline
+    discipline              Discipline  @default(SQUAT)
     date                    DateTime
     index                   Int
     weight                  Int
     raw                     Boolean
@@ -132,9 +136,9 @@
     id                      String      @id  @default(uuid())
     createdAt               DateTime    @default(now())
     updatedAt               DateTime    @updatedAt
     name                    String
-    gender                  Gender
+    gender                  Gender      @default(MALE)
     min                     Float
     max                     Int
 }
@@ -166,9 +170,9 @@
 model OfficialSlot {
     id                      String      @id  @default(uuid())
     createdAt               DateTime    @default(now())
     updatedAt               DateTime    @updatedAt
-    position                Position
+    position                Position    @default(SEITENKAMPFRICHTER)
     official                Official    @relation(fields: [officialId], references: [id])
     officialId              String
     slot                    Slot        @relation(fields: [slotId], references: [id])
```

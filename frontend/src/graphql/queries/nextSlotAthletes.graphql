query nextSlotAthletes($slotId: ID!, $filters: [FilterInput!]) {
    slot(id: $slotId){
        id
        eventId
        nextAthletes(filters: $filters) {
            id
            name
            attempts {
                discipline
                id
                valid
                weight
                done
                index
                resign
            }
        }
    }
}

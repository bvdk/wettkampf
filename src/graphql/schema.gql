# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

type AgeClass {
  id: ID!
  name: String!
}

type Athlete {
  id: ID!
  eventId: String!
  event: Event!
  slotId: String!
  slot: Slot!
  importId: String!
  firstName: String!
  lastName: String!
  gender: String!
  birthday: DateTime!
  club: String!
  norm: Boolean!
  lateRegistration: Boolean!
  price: String!
  ageClass: AgeClass!

  # Body weight in kg
  bodyWeight: Float!
  weightClass: WeightClass!

  # Attempt count
  attemptCount: Int!

  # Calc wilks
  wilks: Float!
  los: Int!
  points: Float!
  total: Float!
  place: Int!
  location: Int!
  attempts: [Attempt!]!
}

type AthleteGroup {
  id: ID!
  slotId: String!
  name: String!
  gender: Gender
  ageClass: AgeClass
  weightClass: WeightClass
}

input AthleteGroupInput {
  name: String
  gender: Gender
  ageClassId: String
  weightClassId: String
}

input AthleteInput {
  importId: String!
  firstName: String!
  lastName: String!
  gender: String!
  birthday: DateTime!
  club: String!
  norm: Boolean!
  lateRegistration: Boolean!
  price: String!
  ageClassId: String!

  # Body weight in kg
  bodyWeight: Float!
  weightClassId: String!

  # Attempt count
  attemptCount: Int!

  # Calc wilks
  wilks: Float!
  los: Int!
  points: Float!
  total: Float!
  place: Int!
  location: Int!
}

type Attempt {
  id: ID!
  weight: Float!
  valid: Boolean!
  done: Boolean!
  date: String!
}

enum ContestType {
  SINGLE
  TEAM
}

# The javascript `Date` as string. Type represents date and time as the ISO Date string.
scalar DateTime

enum Discipline {
  BENCHPRESS
  SQUAT
  DEADLIFT
  POWERLIFTING
}

type Event {
  id: ID!
  name: String!
  discipline: Discipline
  contestType: ContestType
  teamGender: Gender
  teamSize: Int
  groupOrder: [String!]
  currentDiscipline: Discipline
  slots: [Slot!]!
}

input EventInput {
  name: String
  discipline: Discipline
  contestType: ContestType
  teamGender: Gender
  teamSize: Int
  currentDiscipline: Discipline
}

enum Gender {
  MALE
  FEMALE
}

type Mutation {
  createAthleteGroup(slotId: ID!, data: AthleteGroupInput!): AthleteGroup!
  updateAthleteGroup(data: AthleteGroupInput!, id: ID!): AthleteGroup!
  deleteAthleteGroup(id: ID!): AthleteGroup!
  createAthlete(eventId: ID!, athleteGroupId: ID, data: AthleteInput!): Athlete!
  updateAthlete(data: AthleteInput!, id: ID!): Athlete!
  deleteAthlete(id: ID!): Athlete!
  createSlot(eventId: ID!, data: SlotInput!): Slot!
  updateSlot(data: SlotInput!, id: ID!): Slot!
  deleteSlot(id: ID!): Slot!
  createEvent(data: EventInput!): Event!
  updateEvent(data: EventInput!, id: String!): Event!
  deleteEvent(id: String!): Event!
}

type Query {
  athletes: [Athlete!]!

  # Get a event
  event(id: ID!): Event!

  # Get all the events
  events: [Event!]!
}

type Slot {
  id: ID!
  name: String!
  event: Event!
  eventId: String!
  index: Int!
  athletes: [Athlete!]!
  athleteGroups: [AthleteGroup!]!
}

input SlotInput {
  name: String
}

type WeightClass {
  id: ID!
  name: String!
  gender: String!
}

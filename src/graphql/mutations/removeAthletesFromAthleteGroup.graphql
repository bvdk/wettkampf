mutation removeAthletesFromAthleteGroup($athleteGroupId: ID!, $athleteIds: [ID!]!){
    removeAthletesFromAthleteGroup(athleteGroupId: $athleteGroupId, athleteIds: $athleteIds){
        id
        athletes {
            id
            firstName
            lastName
        }
    }
}

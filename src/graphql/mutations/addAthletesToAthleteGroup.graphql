mutation addAthletesToAthleteGroup($athleteGroupId: ID!, $athleteIds: [ID!]! ) {
    addAthletesToAthleteGroup(athleteGroupId: $athleteGroupId, athleteIds: $athleteIds ){
        id
        athletes {
            id
        }
    }
}